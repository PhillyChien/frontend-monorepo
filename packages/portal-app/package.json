{
  "name": "portal-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b && vite build",
    "clean": "rimraf dist",
    "dev": "vite",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "preview": "vite preview",
    "tsc": "tsc -b --verbose"
  },
  "dependencies": {
    "@frontend-monorepo/backend-client": "workspace:*",
    "@frontend-monorepo/i18n": "workspace:*",
    "@frontend-monorepo/tailwind": "workspace:*",
    "@frontend-monorepo/ui": "workspace:*",
    "@frontend-monorepo/utility": "workspace:*"
  }
}
